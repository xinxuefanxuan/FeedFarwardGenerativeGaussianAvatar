data:
  dataset_name: placeholder_dataset
  root: data/
  split:
    train: train
    val: val
    test: test
  loader:
    batch_size: 2
    num_workers: 4
    pin_memory: true
  fields:
    # TODO: 以下字段命名需与真实数据协议确认。
    images: TODO_images_field
    cameras: TODO_cameras_field
    flame: TODO_flame_field
